# Repro of the Vue + Cloudflare Server Build Bug

![Error Screenshot](./screenshot.jpg)

Run `npm run build` to see the above error.  This is a problem when using `@astrojs/vue` together with `@astrojs/cloudflare`.
